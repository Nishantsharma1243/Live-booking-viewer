{
  "name": "live-booking-site",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "nodemon server":"server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "nodemon": "^3.1.10",
    "socket.io": "^4.8.1"
  }
}
